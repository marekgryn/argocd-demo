apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: helm-demo-app2
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/marekgryn/argocd-demo.git
    targetRevision: HEAD
    path: helm-dev
  destination:
    server: https://kubernetes.default.svc
    namespace: helm-demo-app2

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true
